import 'package:dog_breeds/constants/colors/colors.dart';
import 'package:flutter/material.dart';

const titleStyle =
    TextStyle(fontSize: 20, fontWeight: FontWeight.bold, color: darkGrey);
const selectionBarStyle =
    TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: darkGrey);
const emptyWidget = SizedBox.shrink();
 const borderRadius3 = BorderRadius.all(Radius.circular(3));
const borderRadius8 = BorderRadius.all(Radius.circular(8));
const cardRoundedBorder8 = RoundedRectangleBorder(
    borderRadius: borderRadius8,
    side: BorderSide(color: happyBrown, width: 1.5));
const paddingAll8 = EdgeInsets.all(8);

